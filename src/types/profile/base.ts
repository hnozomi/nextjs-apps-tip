export type ProfileContact = {
  email: string;
  github?: string;
  twitter?: string;
  linkedin?: string;
};

export type WorkExperience = {
  company: string;
  position: string;
  period: string;
  description: string;
};

export type EducationHistory = {
  institution: string;
  degree: string;
  period: string;
};
